import { css } from "styled-components";

export const commonInteractiveElementStyling = css`
  border: 0.2rem solid var(--tint);
  color: var(--tint);
  border-radius: 0.2rem;
  background-color: transparent;
  padding: 0.25rem 0.75rem;
`;
